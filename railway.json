{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd frontend && npm run build && cd .. && python -m uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
  }
}
